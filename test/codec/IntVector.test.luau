--!strict
--!optimize 2
local Lync = require(script.Parent.Parent.Parent)
local Runner = require(script.Parent.Parent.Runner)
local describe, it, expect, roundTrip = Runner.describe, Runner.it, Runner.expect, Runner.roundTrip

describe("vec2int16", function(): ()
    it("zero", function(): ()
        local r = roundTrip(Lync.vec2int16, Vector2int16.new(0, 0))
        expect(r.X):toBe(0)
        expect(r.Y):toBe(0)
    end)
    it("positive", function(): ()
        local r = roundTrip(Lync.vec2int16, Vector2int16.new(100, 200))
        expect(r.X):toBe(100)
        expect(r.Y):toBe(200)
    end)
    it("negative", function(): ()
        local r = roundTrip(Lync.vec2int16, Vector2int16.new(-32768, 32767))
        expect(r.X):toBe(-32768)
        expect(r.Y):toBe(32767)
    end)
    it("4 bytes", function(): ()
        local _, w = roundTrip(Lync.vec2int16, Vector2int16.new(0, 0))
        expect(w):toBe(4)
    end)
end)

describe("vec3int16", function(): ()
    it("zero", function(): ()
        local r = roundTrip(Lync.vec3int16, Vector3int16.new(0, 0, 0))
        expect(r.X):toBe(0)
        expect(r.Y):toBe(0)
        expect(r.Z):toBe(0)
    end)
    it("extremes", function(): ()
        local r = roundTrip(Lync.vec3int16, Vector3int16.new(-32768, 0, 32767))
        expect(r.X):toBe(-32768)
        expect(r.Y):toBe(0)
        expect(r.Z):toBe(32767)
    end)
    it("6 bytes", function(): ()
        local _, w = roundTrip(Lync.vec3int16, Vector3int16.new(0, 0, 0))
        expect(w):toBe(6)
    end)
end)

return nil
